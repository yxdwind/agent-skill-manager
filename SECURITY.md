# 安全政策（Security Policy）

## 支持的版本

agent-skill-manager 是一个小型的 GitHub 开源项目。安全修复只针对最新代码（`master` 分支），以及已发布的最新 GitHub Release。旧版本快照不受支持，请及时升级。

## 私下报告漏洞

**请勿**通过公开的 issue 或 pull request 发布漏洞详情、暴露的凭据、私有内容或任何敏感数据。本仓库**不接受公开的安全报告**。

请优先使用 GitHub 的**私有漏洞报告流程**（本仓库 **Security** 标签页，Private vulnerability reporting）。如果私有报告入口不可用，可通过维护者 **Albert** 的 GitHub 主页联系，索取私有报告渠道——注意公开消息中不要包含任何漏洞细节。

私有报告中请包含以下内容：

- 受影响的版本号或 commit；
- 复现问题的最少步骤；
- 预期行为与实际行为；
- 安全与隐私影响；
- 如有可能，附上修复建议。

请移除真实的 API 密钥、访问令牌、私有 URL 与个人信息，使用脱敏值与公开测试内容。

本仓库**不保证响应时间，也没有漏洞赏金计划**。请给予合理的调查与修复时间，再考虑公开披露。

## 高危问题示例

以下类型的问题属于高危，请优先私密报告：

- API 密钥或私有内容出现在源码、日志、截图或发布包中；
- skill 内容中的**提示注入（prompt injection）**或指令覆盖，试图绕过 AI 助手的安全护栏；
- skill 内置脚本中的**危险代码**（如 `curl | sh`、`rm -rf /`、`exec()`、读取 `~/.ssh` 等敏感文件）；
- skill 未经授权向未知网络端点**外发数据**（webhook、`requests.post` 等）；
- 可执行二进制文件（`.exe`/`.dll`）混入 skill 目录；
- 依赖或发布流程被篡改；
- 绕过本地数据删除或权限控制。

## 用户安全指引

- 只从可信来源安装与运行 skill；安装前建议使用 `askill audit` 对 skill 进行安全评测（提示注入 / 危险代码 / 敏感信息 / 二进制文件四维检测）；
- 定期查看 `askill audit` 输出，关注评分与 critical/high 级别发现项；
- 不要执行来源不明的 skill 脚本，尤其是包含"下载后执行"模式（`curl | sh`、`irm | iex`）的 skill；
- 不要将生产环境的 API 密钥、令牌放入 skill 或配置文件；
- 如设备、仓库或截图泄露了凭据，请立即吊销相关密钥。

---

维护者：**Albert**
